import React, { StatelessComponent } from 'react';

export interface AppProps {}

const App: StatelessComponent<AppProps> = () => (
  <h1 className="dn-app-heading">Hello React with TypeScript!</h1>
);

export default App;
